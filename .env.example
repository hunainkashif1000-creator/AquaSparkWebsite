# Copy this file to `.env.local` and fill in your own values.
# NEVER commit real passwords — this repository is public.
MONGODB_URI=mongodb+srv://<dbUser>:<dbPassword>@<clusterName>.mongodb.net/
MONGODB_DB=aqua-spark
